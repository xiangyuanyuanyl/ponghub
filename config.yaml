services:
  - name: "Novel Agent on Render"
    endpoints:
      - url: "https://novel-agent.onrender.com"
        method: "GET"
        timeout: 30
        expect_status: 200

notifications:
  enabled: false
